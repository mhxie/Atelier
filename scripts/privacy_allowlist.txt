# Deliberate opt-outs for scripts/privacy_check.py.
#
# Each line is an exact private-title candidate that is intentionally public
# in this repository. Use this only for generic standards, product names,
# schema keys, illustrative concepts, or user-approved infrastructure examples.
# Personal names, places, preferences, account labels, and secrets belong in
# `profile/private_terms.txt`, never here.
# Matching is case-insensitive. Comments start with `#`; blank lines are
# ignored.
#
# Example:
#   Sample Wiki Entry Title

# Scribe operation name (used in capture-fast-path table); not a private
# filename stem despite the regex collision with `<paths.people>/Person Stub.md`.
people stub

# Public products, standards, libraries, and general concepts used by the
# shipped implementation or documentation.
AWS IAM
Career Plan
Confirmation Bias
Drive ZK Ingestion
drive-zk-ingestion
First Principles Thinking
Meeting Notes
Operating System
Readwise Reader
Root Cause Analysis
Scikit-learn
Source Quality
# Generic atelier/library vocabulary used by protocols/atelier.md.
Source Register

# Public schema and adapter vocabulary. Both slug and display forms are kept
# because private vault filenames can produce either candidate.
address-history
credentials index
credentials-index
meta-reflection-draft
reader persona
reader_persona
research-profile
signal sweep
signal-sweep

# Public sample infrastructure identifiers explicitly approved by the user.
atelier-mbp

