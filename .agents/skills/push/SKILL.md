---
name: push
description: "Run the Atelier `/push` workflow in Codex using the shared command registry. Use when the user explicitly invokes `$push`. Publish local commits after both privacy gates pass over the whole unpushed range."
---

<!-- GENERATED by scripts/render_runtime_edges.py from harness/commands.toml; edit the registry and re-render. -->

## Atelier Command

Run the explicit Codex `$push` skill. Its authoritative workflow source is
the Claude Code `/push` command specification.

1. Read `AGENTS.md` and `CLAUDE.md`.
2. Read the authoritative `.claude/commands/push.md` command specification directly.
3. Execute it in this thread using the Codex adaptation table in `AGENTS.md`.
4. Treat text following `$push` as command context or arguments.
5. Do not start a nested Codex process; complete the workflow in this thread.
