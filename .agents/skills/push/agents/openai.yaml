# GENERATED by scripts/render_runtime_edges.py; edit harness/commands.toml and re-render.
interface:
  display_name: "Atelier $push"
  short_description: "Run the Atelier $push skill in Codex"
  default_prompt: "Use $push to run the matching Atelier command."

policy:
  allow_implicit_invocation: false
