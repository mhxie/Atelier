---
name: triage
description: "Run the Atelier `/triage` workflow in Codex using the shared command registry. Use when the user explicitly invokes `$triage`. Unified operational review center with a read-only overview and bounded review batches."
---

<!-- GENERATED by scripts/render_runtime_edges.py from harness/commands.toml; edit the registry and re-render. -->

## Atelier Command

Run the explicit Codex `$triage` skill. Its authoritative workflow source is
the Claude Code `/triage` command specification.

1. Read `AGENTS.md` and `CLAUDE.md`.
2. Read the authoritative `.claude/commands/triage.md` command specification directly.
3. Execute it in this thread using the Codex adaptation table in `AGENTS.md`.
4. Treat text following `$triage` as command context or arguments.
5. Do not start a nested Codex process; complete the workflow in this thread.
